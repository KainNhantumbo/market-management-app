export type ThemeType = {
	primary: string;
	secondary: string;
	alter: string
	alterAlt: string
	font: string;
	inner: string;
	text: string;
	shadows: string;
	hover: string;
	background: string;
	backgroundAlt: string;
}